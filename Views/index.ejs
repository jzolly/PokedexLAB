<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head.ejs') %>
    <title>Pokedex</title>
</head>
<body>
    <div class="container">
        <h1>Pokemon Index</h1>
        <a href="/pokemon/new"><button>Add New Pokemon</button></a>
        <table>
            <tr>
                <th>Image</th>
                <th>Name</th>
                <th>Type</th>
                <th>User Tools</th>
            </tr>
        <% for(let i=0; i<10; i++) { %>
            <tr>
                    <td><img src='<%= pokemon[i].img %>' alt='<%= pokemon[i].name%>' height="60px"/> <br /> <a href="/pokemon/<%=pokemon[i].id; %>/edit">Edit</a></td>
                    <td><a href="/pokemon/<%= pokemon[i].id %>"><%= pokemon[i].name %></a></td>
                    <td><%= pokemon[i].type %></td>
                    <td> <form action="/pokemon/<%=pokemon[i].id; %>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE" />
                      </form>
                       </td>
                </tr>
                <% } %>
            </table>
            <a href="/pokemon/new"><button>Add New Pokemon</button></a>
    </div>
</body>
</html>